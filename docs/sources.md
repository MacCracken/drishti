# drishti — Sources

> Last Updated: 2026-07-10

The citation index for drishti's algorithmic and domain content. AGNOS
codec crates carry a sources file so every nontrivial parse/coding step
is traceable to a spec or primary reference, and so a future maintainer
can re-derive (or audit) any of it. Entries are grouped by module
family. Discipline: the SPEC is the derivation; implementations are
cross-checks — multiple of them, never a single source
(redesign-don't-reinvent).

## Core (`src/drishti.cyr`, `src/bits.cyr`, `src/ivf.cyr`)

- **AV1 Bitstream & Decoding Process Specification** v1.0.0 w/ Errata 1
  (AOMedia) — 4.10.3 uvlc(), 4.10.5 leb128() (the ≤8-byte / ≤32-bit
  conformance bounds `dr_leb128_read` enforces).
- **ITU-T Rec. H.264** — 9.1/9.1.1 ue(v)/se(v) exp-Golomb parsing
  process (shared verbatim by H.265 9.2) — `dr_ue_read`/`dr_se_read`
  and the writer inverses.
- **IVF container** — the de-facto libvpx/AOM test-bench format
  (32-byte `DKIF` header + 12-byte frame headers); layout cross-checked
  against libvpx `ivfdec.c`/`ivfenc.c` and dav1d's `tools/input/ivf.c`.

## AV1

- **"AV1 Bitstream & Decoding Process Specification"** v1.0.0 with Errata 1, Alliance for Open Media, 2019 — authoritative. Sections used through 0.7.1 (cited inline in the modules): 4.10.3 uvlc(), 4.10.5 leb128(), 4.10.6 su(n), 4.10.7 ns(n), 5.3.1 open_bitstream_unit(), 5.3.2 obu_header(), 5.3.3 obu_extension_header(), 5.5.1 sequence_header_obu(), 5.5.2 color_config(), 5.5.3 timing_info(), 5.5.4 decoder_model_info(), 5.5.5 operating_parameters_info(), 5.9.2 uncompressed_header(), 5.9.3 tile_log2(), 5.9.4 get_relative_dist(), 5.9.5 frame_size(), 5.9.6 render_size(), 5.9.7 frame_size_with_refs(), 5.9.8 superres_params(), 5.9.9 compute_image_size(), 5.9.10 read_interpolation_filter(), 5.9.11 loop_filter_params(), 5.9.12 quantization_params(), 5.9.13 read_delta_q(), 5.9.14 segmentation_params(), 5.9.15 tile_info(), 5.9.17 delta_q_params(), 5.9.18 delta_lf_params(), 5.9.19 cdef_params(), 5.9.20 lr_params(), 5.9.21 read_tx_mode(), 5.9.22 frame_reference_mode(), 5.9.23 skip_mode_params(), 5.9.24 global_motion_params(), 5.9.25-5.9.28 read_global_param + subexp decode, 5.9.29 inverse_recenter(), 5.9.30 film_grain_params(), 5.9.31 temporal_point_info(), 6.2.2/6.2.3 OBU semantics (forbidden bit must be 0; reserved bits decoder-ignored), 6.4.1 sequence-header semantics (reserved profiles; reduced implies still), 7.8 set_frame_refs process, 7.12.2 get_qindex, 7.20 reference frame update, 7.21 reference frame loading.
- **dav1d** (VideoLAN) — `src/obu.c` (`dav1d_parse_obus`, `parse_seq_hdr`, `parse_frame_hdr`) — decode cross-check: forbidden-bit rejection, reserved-bit tolerance, sequence- and frame-header field order, the set_frame_refs / global-motion subexp / tile-info branches.
- **libaom** (AOMedia reference codec) — `av1/common/obu_util.c` (`aom_read_obu_header`), `av1/decoder/obu.c` (`read_sequence_header_obu`), `av1/decoder/decodeframe.c` (`read_uncompressed_header`, `setup_frame_size`, `set_frame_refs`) — independent second cross-check of the same surfaces.
- **AV1 symbol (arithmetic) coder** — spec section 8.2 is authoritative for the DECODER: 8.2.2 init_symbol(), 8.2.3 exit_symbol(), 8.2.4 read_bool(), 8.2.5 read_literal(), 8.2.6 read_symbol() (the multi-symbol decode + CDF adaptation), with EC_PROB_SHIFT/EC_MIN_PROB from section 3. The paired ENCODER (the encode-lane seed) is the exact inverse of that decode process; its range-coder normalization + carry flush is cross-checked against **rav1e** `src/ec.rs` (`WriterBase::store` / `done`) and the daala `od_ec_enc` design.
- **AV1 inverse transform** — spec section 7.13 is authoritative (`src/av1_itx.cyr`): 7.13.2.1 butterflies B/H + cos128/sin128/brev + Cos128_Lookup, 7.13.2.2/3 inverse DCT permutation + the 31-step process (sizes 4-64), 7.13.2.4-9 inverse ADST (4/8/16) + permutations, 7.13.2.10 inverse Walsh-Hadamard, 7.13.2.11-15 identity transforms, 7.13.3 the 2D driver; Round2 from section 4; Tx_Width_Log2 / Tx_Height_Log2 / Transform_Row_Shift from the additional-tables annex. Transcribed verbatim from the spec pseudocode and cross-checked by an adversarial spec review.
- **AV1 intra prediction** — spec sections 7.11.2 + 7.11.5 are authoritative (`src/av1_intra.cyr`): 7.11.2.1 predict_intra (reference-sample construction + dispatch + writeback), 7.11.2.2 basic/PAETH, 7.11.2.3 recursive filter-intra, 7.11.2.4 directional (all four angle quadrants), 7.11.2.5 DC, 7.11.2.6 smooth, the intra edge filter/upsample machinery 7.11.2.7/9/10/11/12, and 7.11.5 chroma-from-luma (CfL); Dr_Intra_Derivative / Intra_Edge_Kernel / Sm_Weights / Mode_To_Angle / Intra_Filter_Taps / Tx_Width_Log2 / Tx_Height_Log2 from the additional-tables annex; Round2 / Round2Signed from section 4; the intra-mode enum from the semantics. The CfL alpha derivation (read_cfl_alphas, 5.11.45 → CflAlphaU/V) and MaxLumaW/MaxLumaH are supplied by the block/mode-info decode; the get_filter_type neighbour-mode computation (7.11.2.8) is derived in `av1_residual` (`av1_get_filter_type`, since 0.7.26) from the YModes/UVModes grids and fed to the edge-filter strength. Cross-checked by adversarial spec reviews (non-directional in 0.7.6, directional in 0.7.8, filter-intra in 0.7.9, chroma-from-luma in 0.7.10 — the last against **libaom** `av1/common/cfl.c` (`cfl_luma_subsampling_*`, `subtract_average`, `cfl_predict_lbd`) and **dav1d** `src/ipred_tmpl.c` (`cfl_pred`) as multi-source cross-checks).
- **AV1 dequantization** — spec section 7.12.2 is authoritative (`src/av1_quant.cyr`): the `Dc_Qlookup[3][256]` / `Ac_Qlookup[3][256]` quantizer tables (8/10/12-bit rows), `dc_q`/`ac_q` (the `Q_lookup[(BitDepth-8)>>1][Clip3(0,255,b)]` lookup), `get_qindex` (base_q_idx / delta-q / segment ALT_Q selection), and `get_dc_quant`/`get_ac_quant` (the per-plane `DeltaQ*Dc`/`DeltaQ*Ac`). The tables were transcribed by direct extraction from the spec markdown and cross-checked against **libaom** `av1/common/quant_common.c` (`dc_qlookup`/`ac_qlookup`, `av1_get_dc_quant`/`av1_get_ac_quant`, `get_qindex`); an adversarial review diffed every one of the 2×768 values against the spec. The reconstruct process (7.12.3) that consumes these lands in a later 0.7.x sub-bite.
- **AV1 reconstruct process** — spec section 7.12.3 is authoritative (`src/av1_recon.cyr`): the per-coefficient dequantization (`dq = Quant·q`, `dq2 = sign·((|dq| & 0xFFFFFF)/dqDenom)`, the `±2^(7+BitDepth)` clamp), `dqDenom` (2 for the 32-class, 4 for the 64-class transforms), the 2D inverse transform invocation (7.13.3), and the `flipUD`/`flipLR` (FLIPADST) residual add with `Clip1`. Cross-checked against **libaom** (`av1/decoder/decodeframe.c` `inverse_transform_block` + the `dqcoeff` dequant with the magnitude mask + large-transform shift, and the FLIPADST handling in `av1/common/av1_inv_txfm2d`). The quantizer-matrix path (7.12.3 step 1b) is deferred; the `Quant[]` coefficient array + resolved `q_dc`/`q_ac` are caller inputs until the `coeffs()` decode lands. An adversarial review verified the dequant math, the flip/dqDenom, the transform integration, and the known-answers (reusing the inverse-transform DC/IDTX/WHT vectors as oracles).
- **AV1 coefficient scan orders** — spec section 5.11.41 is authoritative (`src/av1_scan.cyr`): the `get_scan` / `get_default_scan` / `get_mrow_scan` / `get_mcol_scan` selection and the 32 `Default_Scan_*` / `Mrow_Scan_*` / `Mcol_Scan_*` tables from the additional-tables annex. Tables extracted from the spec markdown and cross-checked against **libaom** `av1/common/scan.c` (`av1_scan_orders`, the `default_scan_*` / `mrow_scan_*` / `mcol_scan_*` arrays); an adversarial review diffed every scan value and verified the size/type → scan selection. Each table is validated as a permutation of `0..len-1`. The `coeffs()` reading loop that walks these scans lands in a later 0.7.x sub-bite.
- **AV1 coefficient level contexts** — spec section 8.3.2 (CDF selection process) is authoritative (`src/av1_coeff.cyr`): `get_tx_class`, the `coeff_base` / `coeff_base_eob` context (`get_coeff_base_ctx`), the `coeff_br` context (`get_br_ctx`), and the `eob_pt` context, with the `Coeff_Base_Ctx_Offset` / `Coeff_Base_Pos_Ctx_Offset` / `Sig_Ref_Diff_Offset` / `Mag_Ref_Offset_With_Tx_Class` / `Adjusted_Tx_Size` tables. Cross-checked against **libaom** `av1/common/txb_common.h` (`get_nz_map_ctx` / the base-context offset, `get_br_ctx`, and the reference-offset tables); an adversarial review diffed every `Coeff_Base_Ctx_Offset` value and re-derived the context math. The `txb_skip` / `dc_sign` contexts (which need the per-tile neighbour arrays) and the `coeffs()` reading loop land in later 0.7.x sub-bites.
- **AV1 default coefficient CDFs** — the additional-tables annex is authoritative (`src/av1_coeffcdf.cyr`): all seven initial adaptive-CDF families — `Default_Txb_Skip_Cdf`, `Default_Eob_Pt_{16..1024}_Cdf`, `Default_Eob_Extra_Cdf`, `Default_Dc_Sign_Cdf`, `Default_Coeff_Base_Eob_Cdf`, `Default_Coeff_Base_Cdf`, `Default_Coeff_Br_Cdf` — across the 4 `COEFF_CDF_Q_CTXS` quantizer buckets (15,996 entries). Stored in the symbol coder's forward-cumulative format (N freqs ending in 32768 + a 0 count). Extracted from the spec (evaluating the `128 * x` products) and cross-checked against **libaom** `av1/common/token_cdfs.h` (accounting for libaom's AOM_CDF inverse-value convention); adversarial reviews diffed every value and validated CDF structure.
- **AV1 coefficient reading loop** — spec section 5.11.39 (`coeffs()`) is authoritative (`src/av1_coeffs.cyr`), with the CDF-selection contexts from 8.3.2 (`all_zero`/txb_skip and `dc_sign`) and the `Tx_Size_Sqr` / `Tx_Size_Sqr_Up` tables + `txSzCtx`. `av1_coeffs_decode` reads all_zero, the eob position (`eob_pt` + `eob_extra`), the base levels (`coeff_base` / `coeff_base_eob`), the `coeff_br` range extension, the signs (`dc_sign` / `sign_bit`), and the Exp-Golomb tail; `av1_coeffs_encode` is its exact inverse. Cross-checked against **libaom** `av1/decoder/decodetxb.c` (`av1_read_coeffs_txb`, `read_golomb`) and the reference eob/base/br/golomb control flow. The unary golomb length is capped (libaom-style) so a truncated stream can never hang. Verified by decode/encode round-trip. The **adaptive per-tile CDF context** (`av1_ccdf_*`, mirroring libaom `init_coeff_cdfs` + the per-tile CDF copy that the symbol coder adapts in place) lets `coeffs()` run with `disable_cdf_update = 0`; verified by an adaptive multi-block round-trip.
- **AV1 default non-coefficient CDFs** — the additional-tables annex is authoritative (`src/av1_noncoeffcdf.cyr`): the intra-keyframe default CDF tables — `Default_Partition_W{8,16,32,64}_Cdf`, `Default_Skip_Cdf`, `Default_Intra_Frame_Y_Mode_Cdf`, `Default_Uv_Mode_Cfl_{Not_}Allowed_Cdf`, `Default_Angle_Delta_Cdf`, `Default_Cfl_Sign_Cdf`, `Default_Cfl_Alpha_Cdf`, `Default_Filter_Intra{_Mode}_Cdf`, `Default_Tx_{8x8,16x16,32x32,64x64}_Cdf`, `Default_Intra_Tx_Type_Set{1,2}_Cdf` (1,622 entries). Stored in the symbol coder's forward-cumulative format; extracted from the spec (evaluating `128 * x`) and cross-checked against **libaom** `av1/common/entropymode.c` (accounting for the AOM_CDF inverse-value convention). The block/partition-decode reads that consume these land in later 0.7.x bites. The 7-bite block-decode decomposition was mapped by a multi-agent scoping pass.
- **AV1 intra mode-info reads** — spec section 5.11.16 (`intra_frame_mode_info`) is authoritative (`src/av1_modeinfo.cyr`), with the CDF-selection contexts from the parsing process (spec 9.3): `read_skip` (ctx = `Skips[above]+Skips[left]`), `intra_frame_y_mode` (cdf `[abovemode][leftmode]`, `abovemode`/`leftmode` = `Intra_Mode_Context[avail ? neighbourYMode : DC_PRED]`), `intra_angle_info_y`/`_uv` (angle_delta gated on `MiSize>=BLOCK_8X8` + `is_directional_mode`, cdf index `mode-V_PRED`), `uv_mode` (the Lossless / `Max(BW,BH)<=32` CfL-allowed cdf selection via `get_plane_residual_size`), `read_cfl_alphas` (joint sign `signU=(signs+1)/3`, `signV=(signs+1)%3`; magnitude ctx `(signU-1)*3+signV` / `(signV-1)*3+signU`, spec 5.11.45), and `filter_intra_mode_info` (`enable_filter_intra && YMode==DC_PRED && Max(BW,BH)<=32`). Decode functions plus their exact-inverse encoders (round-trip tested through the symbol coder). Ships with the block-size conversion tables — `Mi_Width/Height_Log2`, `Num_4x4_Blocks_Wide/High`, `Block_Width/Height`, `Size_Group`, `Intra_Mode_Context`, `Subsampled_Size` — from the additional-tables annex. A 5-agent adversarial review (syntax/read-order, CDF-selection contexts, per-value block-table diff, encode/decode inversion, hostile-input safety) confirmed it against the spec markdown with no findings. Feature-gated reads (segmentation/cdef/delta-q-lf/intrabc/palette) are deferred to later bites.
- **AV1 intra transform-size read** — spec section 5.11.15 (`read_tx_size`) + the `read_block_tx_size` intra branch (5.11.17) are authoritative (`src/av1_txsize.cyr`), with the `tx_depth` CDF-selection context from the parsing process (spec 9.3). `av1_read_tx_size` returns the block's luma `TxSize` (`Lossless` → `TX_4X4`; else start at `Max_Tx_Size_Rect[MiSize]` and apply `Split_Tx_Size` once per `tx_depth` when `MiSize>BLOCK_4X4 && allowSelect && TxMode==TX_MODE_SELECT`); the ctx is `(aboveW>=maxTxWidth)+(leftH>=maxTxHeight)` where, for an intra keyframe (`IsInters=0`), `aboveW`/`leftH` reduce to `Tx_Width/Height[InterTxSizes[neighbour]]`; the `maxTxDepth → Tx8/16/32/64` cdf-family dispatch consumes the 0.7.19 CDFs. `av1_write_tx_size` derives `tx_depth` back from the target `TxSize` (exact inverse). Tables `Max_Tx_Size_Rect` / `Max_Tx_Depth` / `Split_Tx_Size` from the additional-tables annex (cross-checkable against **libaom** `av1/common/common_data.h` `max_txsize_rect_lookup` / `sub_tx_size_map` and `av1/common/blockd.h`). A 4-agent adversarial review (read_tx_size fidelity, tx_depth ctx + cdf dispatch, per-value table diff, hostile-input safety) confirmed it against the spec with no findings. The inter `read_var_tx_size` tree and the `InterTxSizes` grid write are deferred to later bites.
- **AV1 transform-type derivation** — spec sections 5.11.48 (`transform_type` / `get_tx_set`) + 5.11.40 (`compute_tx_type`) are authoritative (`src/av1_txtype.cyr`), with the `intra_tx_type` CDF-selection context from the parsing process (spec 9.3). `get_tx_set` maps `(Tx_Size_Sqr, Tx_Size_Sqr_Up, is_inter, reduced_tx_set)` to the tx set (DCTONLY / INTRA_1 / INTRA_2, and the INTER_1/2/3 for faithfulness). `av1_transform_type_decode` reads `intra_tx_type` for a luma block when `set>0 && qindex>0`, via `av1_ncdf_intra_tx_set1/2[Tx_Size_Sqr[txSz]][intraDir]` (`intraDir` = `Filter_Intra_Mode_To_Intra_Dir[filter_intra_mode]` when filter-intra is used, else `YMode`), mapping through `Tx_Type_Intra_Inv_Set1/2`; `av1_transform_type_encode` is the exact inverse. `compute_tx_type` forces `DCT_DCT` for lossless/`txSzSqrUp>TX_32X32`, returns the luma value for plane 0, and for chroma uses `Mode_To_Txfm[UVMode]` gated by `Tx_Type_In_Set_Intra`. Spliced into the `coeffs()` loop (`src/av1_coeffs.cyr`) between `all_zero` and the eob position, retiring `PlaneTxType` as a caller input. Tables from the additional-tables annex + spec syntax; cross-checkable against **libaom** `av1/common/*` (`mode2txfm_map`, `av1_ext_tx_inv`, `av1_ext_tx_used`, `get_ext_tx_set`, `fimode_to_intradir`). A 5-agent adversarial review (transform_type fidelity, compute_tx_type/get_tx_set, per-value tables, the coeffs splice + Tx_Size_Sqr move, hostile-input safety) confirmed it against the spec with no findings. Inter chroma co-location + `Tx_Type_In_Set_Inter` and segmentation qindex are deferred to later bites.
- **AV1 residual driver** — spec sections 5.11.34 (`residual()`) + 5.11.36 (`transform_block` / `get_tx_size`) are authoritative (`src/av1_residual.cyr`), with the `clear_block_decoded_flags` process (5.11.3) for the `BlockDecoded` grid. `av1_transform_block` composes the already-tested intra primitives: `av1_intra_predict` (7.11.2) for the prediction (mode = `YMode` for luma, `UVMode` or `DC_PRED`+`av1_predict_chroma_from_luma` for a CfL chroma block), then — unless `skip` — `av1_coeffs_decode` (5.11.39, which computes the `PlaneTxType` internally) and, when `eob>0`, `av1_reconstruct` (7.12.3). It threads the `BlockDecoded` availability grid (the `haveAboveRight`/`haveBelowLeft` neighbour reads) and the luma `MaxLumaW/H` extents that CfL consumes. `av1_residual` iterates `transform_block` over each plane's tx grid (one 64x64 superblock chunk for the baseline). `av1_get_tx_size` picks the per-plane tx size (the chroma 64→32 clamp). Cross-checkable against **libaom** `av1/decoder/decodeframe.c` (`decode_reconstruct_tx` / `predict_and_reconstruct_intra_block` / `read_coeffs_txb`). A 5-agent adversarial review (transform_block orchestration, residual loop + get_tx_size, BlockDecoded + availability, coordinate/subsampling math, hostile-input safety) confirmed it against the spec with no findings. The 128x128-superblock chunk split, palette, inter `transform_tree`, and per-plane quantizer deltas are deferred.
- **AV1 partition tree** — spec sections 5.11.4 (`decode_partition`) + 5.11.5 (`decode_block`) are authoritative (`src/av1_partition.cyr`), with the partition / `split_or_horz` / `split_or_vert` CDF-selection from the parsing process (spec 9.3). `av1_decode_partition` reads the partition symbol (`av1_ncdf_partition_w8/16/32/64` by `bsl = Mi_Width_Log2[bSize]`; `split_or_horz`/`split_or_vert` use a binary CDF *synthesized* from the partition CDF by summing the vertical-/horizontal-splitting partition probabilities) and recurses / dispatches all 10 partition types via `Partition_Subsize`. `av1_decode_block` derives `HasChroma` + the availability flags, orchestrates `intra_frame_mode_info` → `read_block_tx_size` → `residual()`, and writes the `MiSizes`/`YModes`/`Skips`/`InterTxSizes` grids (clamped to the frame MI extent). The paired `av1_encode_partition`/`av1_encode_block` mirror the traversal so the tree round-trips. Cross-checkable against **libaom** `av1/decoder/decodeframe.c` (`decode_partition` / `decode_mbmi_block` / `read_tx_size`) and **dav1d** `src/decode.c` (`decode_sb` / `decode_b`). A 5-agent adversarial review confirmed the dispatch/ctx/symmetry with **one real finding — an out-of-bounds MI-grid write for an edge coding block on a non-superblock-aligned frame — fixed by clamping the write to the tile extent (spec-equivalent, since the reference decoders pad the MI arrays and the overhang cells are never read as neighbours)**. Partition W128 / 128x128 superblocks, palette, and segmentation are deferred. (`get_filter_type` is derived in `av1_residual` as of 0.7.26.)
- **AV1 tile / frame loop** — spec section 5.11.2 (`decode_tile`) + the `tile_group_obu` wiring (5.11.1) are authoritative (`src/av1_tile.cyr`), with `clear_above_context` / `clear_left_context` (7.x semantics) and the `init_coeff_cdfs` quantizer bucket (`base_q_idx` thresholds 20/60/120). `av1_decode_tile` runs `clear_above_context`, then per superblock row `clear_left_context` and per 64x64 superblock `clear_block_decoded_flags` + `decode_partition`. `av1_decode_intra_tile` builds the per-tile adaptive CDF contexts (`av1_ncdf_new` / `av1_ccdf_new(qbucket)`) + the symbol decoder (`init_symbol` / `exit_symbol`) and drives the loop into a `DrFrame`. The paired `av1_encode_tile` / `av1_encode_intra_tile` mirror it so a keyframe tile round-trips. Cross-checkable against **libaom** `av1/decoder/decodeframe.c` (`decode_tile` / `av1_init_above_context` / `av1_zero_left_context`) and **dav1d** `src/decode.c` (`dav1d_decode_tile_sbrow`). A 4-agent adversarial review (SB loop, CDF/symbol/qbucket wiring, clear-context, encode-symmetry + safety) confirmed it against the spec with no findings. **This closes the intra still-picture decode milestone — profile-0 AV1 keyframes decode end-to-end to pixels.** CDEF / loop-restoration / delta-q per-SB reads, multi-tile, and the frame-end CDF save/average + `decode_frame_wrapup` are deferred to the inter/conformance work.
- **AV1 deblocking loop filter** — spec sections 7.14.3 (filter size) / 7.14.4-5 (adaptive filter strength → lvl, limit, blimit, thresh) / 7.14.6 (sample filtering — mask, narrow 4-tap, wide low-pass) are authoritative (`src/av1_deblock.cyr`). `av1_lf_strength` derives the per-edge filter level from `loop_filter_level` + the ref/mode deltas (intra keyframe: `ref = INTRA_FRAME`, `mode_type = 0`); `av1_lf_limits` derives the thresholds from the level + `loop_filter_sharpness`; `av1_lf_mask` computes the hev/filter/flat/flat2 masks; `av1_lf_narrow` / `av1_lf_wide` are the 4-tap and 8/14-tap kernels; `av1_lf_sample` dispatches. The narrow filter's `filter1`/`filter2` use the sign-preserving `>>>` (spec's arithmetic `>>3` over a signed, frequently-negative intermediate). Cross-checkable against **libaom** `av1/common/av1_loopfilter.c` (`aom_lpf_*`, `filter4` / `filter8` / `filter14`) and **dav1d** `src/loopfilter_tmpl.c`. The edge loop + main driver (spec 7.14.1/2, added 0.7.28) — `av1_lf_edge` (dx/dy, onScreen, the `row|subY`/`col|subX` adjustment, `isBlockEdge`/`isTxEdge`/`applyFilter`, filter size + strength, the per-`MI_SIZE` sample loop) and `av1_deblock` (all vertical then horizontal boundaries per plane, in place) — walk the reconstructed frame's 4x4 edges, reading the `LoopfilterTxSizes` grid (written per tx block in `transform_block`) for the tx-edge / filter-size decisions. Two adversarial reviews (kernels; edge-loop + LoopfilterTxSizes/safety) verified it against the spec — the first independently flagged the narrow-filter arithmetic-shift (confirmed fixed).
- **AV1 CDEF (Constrained Directional Enhancement Filter)** — spec sections 7.15.2 (direction process) / 7.15.3 (`constrain` + CDEF filter process) are authoritative (`src/av1_cdef.cyr`, kernels added 0.7.29). `av1_cdef_direction` runs the 8-direction `partial[8][15]`/`cost[8]` search — `x = (sample >> (BitDepth-8)) - 128`, the eight `partial` accumulations, the per-direction cost sums scaled by `Div_Table`, argmax → `yDir`, and `var = (bestCost - cost[(yDir+4)&7]) >> 10`. `av1_cdef_constrain` is the damped clamp-toward-zero non-linearity (`dampingAdj = Max(0, damping - FloorLog2(threshold))`; `sign * Clip3(0, |diff|, threshold - (|diff| >> dampingAdj))`). `av1_cdef_filter` is the per-8x8 tap filter: `tapIdx = (priStr >> coeffShift) & 1`, a primary tap along `dir` and secondary taps along `(dir±2)&7` (both scaled by the `priStr`-indexed tap tables — spec indexes `Cdef_Sec_Taps` with `priStr`, not `secStr`), `is_inside_filter_region` gating on `MiCols`/`MiRows`, min/max clamp over available samples, and the sign-preserving `x + ((8 + sum - (sum<0)) >>> 4)` round (arithmetic `>>` in the spec — a logical shift would corrupt the negative-`sum` case). Tables `Div_Table` / `Cdef_Pri_Taps` `{{4,2},{3,3}}` / `Cdef_Sec_Taps` `{{2,1},{2,1}}` / `Cdef_Directions[8][2][2]` / `Cdef_Uv_Dir[2][2][8]` are transcribed and element-by-element verified against the spec. Cross-checkable against **libaom** `av1/common/cdef_block.c` (`cdef_find_dir`, `cdef_filter_block`) and **dav1d** `src/cdef_tmpl.c`. Values additionally cross-checked against an independent Python model. A 3-agent adversarial review (tables / direction+constrain / filter+memory-safety) confirmed spec-exactness with no correctness findings; two non-correctness findings tracked — the MI-grid-vs-frame-width frame contract the 0.7.30 driver must satisfy (documented as a precondition), and the codebase-wide per-block bump-allocator scratch pattern (arena-strategy question for the audit arc). The **process (7.15) + block process (7.15.1)** driver landed in 0.7.30 (`av1_cdef_process` / `av1_cdef_block`): the outer loop walks every 8x8 block (step 2 MI units) reading its 64x64-aligned `CdefIdx`; the block process copies `CurrFrame`->`CdefFrame` (luma + chroma), returns on `idx == -1` or all-four-sub-blocks-skipped, else runs the direction search + the luma strength derivation (`dir` from the pre-var-scaled `priStr`; `priStr = var ? (priStr*(4+varStr)+8)>>4 : 0`; `varStr = (var>>6) ? Min(FloorLog2(var>>6),12) : 0`; `damping = CdefDamping + coeffShift`) + the filter, then the chroma path (uv strengths, `Cdef_Uv_Dir[subx][suby][yDir]` remap, `damping-1`). The MI-grid frame contract (finding 1) is enforced by `av1_cdef_coverage_ok` (reject-not-OOB) + `av1_cdef_frame_new` (border >= 8). A 2-agent review (7.15/7.15.1 conformance; coverage guard + memory safety) confirmed the driver with no conformance findings and one latent-OOB fix (bounding an unvalidated `CdefIdx` before indexing the frame-header strength arrays). Cross-checkable against **libaom** `av1/common/cdef.c` (`av1_cdef_frame` / `cdef_filter_fb`) and **dav1d** `src/cdef_apply_tmpl.c`. The **`read_cdef` (5.11.56) / `clear_cdef`** syntax primitives — the per-64x64 `cdef_idx` bitstream read (`L(cdef_bits)`, once per 64x64 guarded by `cdef_idx==-1`, gated on skip / CodedLossless / !enable_cdef / allow_intrabc, filling the block's 4x4 extent at `cdefSize4 = 16` step) plus the paired `write_cdef` — landed in 0.7.31 (`av1_read_cdef` / `av1_write_cdef` / `av1_clear_cdef` in `av1_modeinfo.cyr`), pointer-based and round-trip tested. 0.7.32 wired them into `intra_frame_mode_info` (`read_cdef`/`write_cdef` spliced after `read_skip`, spec 5.11.16 order) + the `decode_tile`/`encode_tile` superblock loop (`clear_cdef`), guarded by a per-tile CDEF context (`av1_tile_set_cdef_ctx`) so non-CDEF streams stay byte-identical. The mechanism is round-trip tested at the mode-info level; production activation — the `set_cdef_ctx`-from-frame-header call — landed in 0.7.41 (`av1_activate_intra_filters`, `src/av1_decode.cyr`), leaving only the OBU / `tile_group_obu` walk that feeds real tile bytes into the path.
- **AV1 loop restoration (Wiener path)** — spec sections 7.17.5 (Wiener filter) / 7.17.6 (Wiener coefficient) / 7.17.7 (get source sample) + 7.11.3.2 (rounding variables) are authoritative (`src/av1_lr.cyr`, Wiener kernels added 0.7.33). `av1_lr_wiener_coeff` expands the 3 coded taps to the 7-tap symmetric, unit-DC-gain filter (`filter[3]=128`; `filter[i]=filter[6-i]=coeff[i]`; `filter[3]-=2*coeff[i]`; chroma's `coeff[0]==0` gives a 5-tap filter). `av1_lr_get_source_sample` clamps to `[0,PlaneEndX]x[0,PlaneEndY]` and routes by the stripe boundary — within-stripe samples from `UpscaledCdefFrame` (post-CDEF), outside from `UpscaledCurrFrame` (pre-CDEF), with the 3rd-line-above/below cropped to the 2nd. `av1_lr_wiener_filter` runs two 1-D 7-tap convolutions — horizontal into `intermediate[(h+6)x w]` (clipped to `[-offset, limit-offset]` where `offset = 1<<(BitDepth+FILTER_BITS-InterRound0-1)`), then vertical into `LrFrame` with `Round2`/`Clip1` — using `InterRound0/InterRound1` (isCompound=0: 3/11, or 5/9 at 12-bit). Cross-checkable against **libaom** `av1/common/restoration.c` (`wiener_filter_stripe`) and **dav1d** `src/looprestoration_tmpl.c` (`wiener`). Values additionally cross-checked against an independent Python model; a 1-agent review confirmed spec-exactness with no bugs. The **self-guided / SGR** filter (7.17.2 + 7.17.3, added 0.7.34) is the second LR path: `av1_lr_box_filter` builds the `A`/`B` box statistics over a 1-sample boundary (`n=(2r+1)^2`; the `s`/`oneOverN` integer division constants; `a2` via the `z`-indexed reciprocal `((z<<SGR_BITS)+z/2)/(z+1)` with the 256/1 boundary cases; `b2=((1<<SGR_BITS)-a2)*b*oneOverN` using the RAW box sum) then the pass-dependent 3x3 weighted output; `av1_lr_self_guided` runs the two passes and projects `Round2(w1*u + w0*{flt0|u} + w2*{flt1|u}, RST_BITS+PRJ_BITS)` with the `Sgr_Params` table (SGRPROJ constants MTABLE 20 / SGR 8 / RECIP 12 / RST 4 / PRJ 7). Cross-checkable against **libaom** `av1/common/restoration.c` (`av1_selfguided_restoration_c` / `boxsum`) and **dav1d** `src/looprestoration_tmpl.c` (`sgr_*`). A 1-agent review of the box filter confirmed spec-exactness (z==255 boundary, raw-b-not-d, negative parity, shifts) with no bugs. The **driver** (7.17.1 process + 7.17.2 loop_restore_block, added 0.7.35) copies `UpscaledCdefFrame` -> `LrFrame` then per 4x4 block dispatches the Wiener/SGR kernel by unit type: `av1_lr_restore_block` derives `StripeStartY = (stripeNum*64 - 8) >>> subY` (arithmetic shift — negative for stripe 0, routing get_source_sample between curr/cdef), the unit indices (`unitRow` with the `+8` offset, `unitCol` without), and the block extent, via `count_units_in_frame` (5.11.57) and the `Av1LrParams` per-unit grids. Cross-checkable against **libaom** `av1/common/restoration.c` (`av1_loop_restoration_filter_frame` / `loop_restoration_filter_unit`) and **dav1d** `src/lr_apply_tmpl.c`. A 1-agent review verified all 7 driver geometry points (esp. the arithmetic shift + the unitRow/unitCol asymmetry) with no dispatch bugs. `read_lr` (5.11.57) populating `Av1LrParams` from the bitstream is the next bite.
- **AV1 subexponential coding (symbol-coder "_bool" family)** — spec section 5.11.57 (`read_lr` / `read_lr_unit`) defines `decode_subexp_bool` / `decode_unsigned_subexp_with_ref_bool` / `decode_signed_subexp_with_ref_bool` (authoritative for `src/av1_symbol.cyr`, added 0.7.36). These are the arithmetic-coded counterparts of the frame-header f/ns subexp coder (5.9.25-5.9.28, `av1_decode_subexp` in `av1_frame.cyr`) — structurally identical but reading `L(n)` (`av1_sym_read_literal`) and `NS(n)` (`av1_sym_read_ns`, matching `dr_ns_read`'s `w=FloorLog2(n)+1`, `m=(1<<w)-n` form) instead of raw bits, with `k` a parameter. `av1_recenter` is the exact inverse of `av1_inverse_recenter` (5.9.29), used by the encode mirrors. Cross-checkable against **libaom** `aom_dsp/binary_codes_reader.c` (`aom_read_primitive_subexpfin` / `inv_recenter_finite_nonneg`) and the spec's own frame-header subexp. A 1-agent review brute-forced bit-pattern equality against the spec with no bugs. These feed `read_lr_unit` (5.11.57, added 0.7.37 in `av1_lr.cyr`): `av1_read_lr_unit` reads the restoration type via the frame-type-gated CDF (`use_wiener`/`use_sgrproj` 2-symbol or `restoration_type` 3-symbol — defaults `{11570}`/`{16855}`/`{9413,22581}` from 10.md), then the Wiener taps (per pass, `firstCoeff = plane?1:0`, `decode_signed_subexp_with_ref_bool` over the `Wiener_Taps_Min/Max/K` tables + the `RefLrWiener` predictor) or the SGR `lr_sgr_set` + the two `LrSgrXqd` weights (subexp, with the `radius==0`/`i==1` `Clip3((1<<SGRPROJ_PRJ_BITS)-RefSgrXqd[0])` computed case). The `RefLrWiener`/`RefSgrXqd` predictor resets per tile to `Wiener_Taps_Mid` `{3,-7,15}` / `Sgrproj_Xqd_Mid` `{-32,31}`. Cross-checkable against **libaom** `av1/decoder/decodeframe.c` (`read_wiener_filter` / `read_sgrproj_filter`). A 1-agent review verified all 6 checkpoints with no bugs. The **`read_lr` per-superblock geometry** (added 0.7.38) — `av1_read_lr`/`av1_write_lr` compute the restoration-unit range a superblock overlaps (`unitRowStart = (r*(MI_SIZE>>subY)+unitSize-1)/unitSize`, `unitRowEnd = Min(unitRows, …)`; the column range scaled by `SuperresDenom`/`SUPERRES_NUM` when `use_superres`) per plane, then dispatch `read_lr_unit` over the half-open unit range. A 1-agent review verified all 8 geometry checks (division grouping, superres branch, Min clamps, read/write parity) with no bugs. This completes loop-restoration bitstream parsing. The **`decode_tile` wiring** (spec 5.11.2, added 0.7.39) splices `read_lr` into the superblock loop (after `clear_cdef` + `clear_block_decoded`, before `decode_partition`) with the per-tile `av1_lr_ref_reset`, guarded by the tile's `AV1TILE_LRPARAMS` — and, unlike the CDEF wiring, is round-trip-tested end-to-end through `decode_tile`. `av1_lr.cyr` was moved before `av1_tile.cyr` in the include chain so the tile loop can call the read_lr functions. Loop restoration is now complete through the decode-tile layer. The **frame-header filter activation** (0.7.41, `src/av1_decode.cyr`) closes the wiring gap: `av1_lr_params_from_fh` builds the `Av1LrParams` straight from the parsed header (5.9.20/7.17) and `av1_activate_intra_filters` attaches it — plus the CDEF read context — to a decode tile, end-to-end tested by decoding a keyframe tile whose LR params were header-derived (the Wiener coeffs the encoder wrote read back). Cross-checkable against **libaom** `av1/decoder/decodeframe.c` (the `decode_tile` restoration bootstrap). Only the OBU + `tile_group_obu` walk that feeds real tile bytes into this path remains.
- **AV1 in-loop filter pipeline** — spec section 7.4 (decoding process: the order deblocking 7.14 -> CDEF 7.15 -> upscaling/loop-restoration 7.17) is authoritative (`src/av1_decode.cyr`, `av1_apply_loop_filters`, added 0.7.40). It runs the three filters in sequence over a reconstructed tile frame, managing the intermediate frames (deblock in place on CurrFrame; CDEF CurrFrame -> CdefFrame; LR (CurrFrame, CdefFrame) -> LrFrame), gated by `av1_seq_enable_cdef` / `av1_fh_uses_lr`. Cross-checkable against **libaom** `av1/decoder/decodeframe.c` (`decode_frame` post-decode filter sequence: `av1_loop_filter_frame` -> `av1_cdef_frame` -> `av1_superres_upscale` -> `av1_loop_restoration_filter_frame`) and **dav1d** `src/recon_tmpl.c` / `src/lf_apply_tmpl.c`. A 3-dimension adversarial review workflow (spec-order / frame-management / memory-error) confirmed the pipeline and found a latent `av1_deblock` bug (missing the 7.14.1 frame-level gate for the level-0 + delta-enabled corner) — fixed at the source.
- **AV1 tile-group OBU parsing** — spec sections 5.11.1 (`tile_group_obu`) + 4.10.4 (`le(n)`) are authoritative (`src/av1_decode.cyr`, `av1_tile_group_parse` / `av1_read_le`, added 0.7.42). The parser reads the group header (`tile_start_and_end_present_flag` gated on `NumTiles > 1`; `tg_start`/`tg_end` via `f(TileColsLog2 + TileRowsLog2)`), `byte_alignment()`, then walks `TileNum` from `tg_start` to `tg_end`: non-last tiles carry a `tile_size_minus_1 = le(TileSizeBytes)` prefix (data follows), the last tile takes the remaining `sz`. It fills an `Av1TileGroup` with each tile's data byte offset + size (absent tiles keep offset -1) that the frame driver hands to `decode_intra_tile`. Trust no input byte: `NumTiles` is capped at `MAX_TILE_COLS*MAX_TILE_ROWS` (64×64), `TileSizeBytes` to 1..4, `tg_start`/`tg_end` validated against `NumTiles` BEFORE any array index (a crafted `tg_end` from `f(tileBits)` cannot OOB), and every size field + tile extent bounds-checked against `sz` (lying/truncated sizes → `DR_ERR_TRUNCATED`/`DR_ERR_BAD_HEADER`, never an OOB read). The cursor-based `sz` tracking is equivalent to the spec's `sz -= tileSize + TileSizeBytes` decrement — the last tile receives exactly the spec's remaining bytes. Cross-checkable against **libaom** `av1/decoder/obu.c` (`read_tile_group_header` / `av1_decode_tg_tiles_and_stats`) and **dav1d** `src/obu.c` (`parse_obus` tile-group case). A 3-dimension adversarial review workflow (spec-conformance / input-safety / offset-arithmetic, each finding refute-by-default verified) confirmed the parser with no correctness findings.
- **rav1e** (Xiph) — encode-lane lineage; the 0.7.x encoder bring-up derives its structure from rav1e's keyframe path rather than libaom's, and the symbol-encoder byte/carry mechanics mirror `WriterBase` in `src/ec.rs`.

## H.264/AVC

- **ITU-T Rec. H.264 — Advanced video coding for generic audiovisual services** — AUTHORITATIVE. Sections used: 7.3.1/7.4.1 + Table 7-1 (NAL unit syntax/semantics), 7.4.1.1 (emulation prevention encapsulation), Annex B B.1.1/B.1.2 (byte-stream format, zero_byte, leading/trailing_zero_8bits), 7.3.2.1.1/7.4.2.1.1 (SPS syntax/semantics, CropUnitX/Y, Table 6-1 SubWidthC/SubHeightC), 7.3.2.1.1.1/7.4.2.1.1.1 (scaling_list, delta_scale range), 7.3.2.2/7.4.2.2 (PPS), 9.1/9.1.1 (ue(v)/se(v) exp-Golomb).
- **RFC 6184 — RTP Payload Format for H.264 Video** — §1.3 NAL header field recap; cross-check for header bit layout and type semantics.
- **openh264** (Cisco) — `codec/decoder/core/src/au_parser.cpp` ParseSps/ParsePps and bit-reader EPB handling; primary decoder cross-check (the implementation drishti's H.264 family replaces).
- **x264** — SPS/PPS serialization order (`encoder/set.c`); encode-side field-order cross-check.
- **JM reference software** (ITU/ISO) — `ldecod` InterpretSPS/InterpretPPS; reference-decoder semantics cross-check.
- **ffmpeg** — `libavcodec/h2645_parse.c`; start-code resync and 4-byte zero_byte attribution behavior cross-check.

## H.265/HEVC

- **ITU-T Rec. H.265 (HEVC)** — authoritative. Sections used at 0.7.0: 7.3.1.2 / 7.4.2.2 (two-byte NAL unit header semantics), Table 7-1 (nal_unit_type), Annex B B.2.2 (byte-stream NAL unit decoding), 7.3.1.1 / 7.4.2 (emulation_prevention_three_byte), 7.3.3 / 7.4.4 (profile_tier_level incl. the 43+1 reserved constraint tail), 7.3.2.1 / 7.4.3.1 (VPS), 7.3.2.2 / 7.4.3.2.1 (SPS + conformance-window crop math), 7.3.2.3 / 7.4.3.3.1 (PPS), Table 6-1 (SubWidthC / SubHeightC), A.4.1 / A.4.2 + Table A.8 (level limits — the dimension bomb guard).
- **libde265** (`nal.cc`, `nal-parser.cc`, `vps.cc`, `sps.cc`, `pps.cc`) — cross-check for NAL header layout, EPB behavior (incl. trailing-EPB strip), and parameter-set field ordering. The library this family replaces.
- **ffmpeg** (`libavcodec/h2645_parse.c`, `hevc_ps.c`, `hevc_parse.c`) — second cross-check for Annex-B scanning and PS parsing; drishti is deliberately stricter (rejects non-zero bytes before start codes instead of resyncing).
- **HM reference model** — planned conformance-vector source for the 0.9.x battery (not yet consumed).
- Real-stream sanity anchor: parameter-set headers pack to the canonical on-wire bytes `40 01` (VPS) / `42 01` (SPS) / `44 01` (PPS), asserted in the suite.

## VP8/VP9

- **RFC 6386 — "VP8 Data Format and Decoding Guide"** — authoritative
  for VP8, and code-as-spec (it carries the reference implementation).
  Sections used at 0.7.0: 7.1 (range-subdivision theory), 7.3
  (bool_decoder / bool_encoder / read_literal / flush_bool_encoder /
  add_one_to_output — ported verbatim, then bounds-hardened), 9.1
  (uncompressed data chunk: frame tag, start code, dimensions), 9.2
  (version number semantics, 0-3 defined), 9.x sign-magnitude coding
  of signed header fields (e.g. 9.6 delta_q).
- **"VP9 Bitstream & Decoding Process Specification"** v0.7
  (Grange / de Rivaz / Hunt, Google) — authoritative for VP9. Sections
  used: 6.2 uncompressed_header(), 6.2.1 frame_sync_code(), 6.2.2
  color_config(), 6.2.3 frame_size(), 6.2.4 render_size(), 7.2 frame
  type semantics, 7.2.2 color_space enumeration, 9.2 bool decoding
  (identical coder to VP8's — one implementation serves both).
- **libvpx** — `vp8/decoder/dboolhuff.h` (`vp8dx_decode_bool`),
  `vp8/encoder/boolhuff.c` (`vp8_encode_bool`),
  `vp9/decoder/vp9_decodeframe.c` (`read_uncompressed_header`,
  `read_bitdepth_colorspace_sampling`) — cross-check for the coder
  arithmetic, carry propagation, and the VP9 header rejection set
  ("4:4:4 color not supported in profile 0 or 2"). The library this
  family replaces.
- **ffmpeg** — `libavcodec/vp8.c` / `vp9.c` header parsers — second
  cross-check of field order and the profile bit split.
